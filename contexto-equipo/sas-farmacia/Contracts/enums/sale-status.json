{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SaleStatusEnum",
  "description": "Shared enum for sale statuses across modules (POS, Invoicing, Orchestrator)",
  "type": "string",
  "enum": ["pending", "paid", "cancelled", "refunded"]
}
